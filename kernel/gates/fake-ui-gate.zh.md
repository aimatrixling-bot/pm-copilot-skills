# Fake UI Gate

原型、前端实现和演示都需要使用这个门禁。

## 检查项

- 按钮要么可用，要么清楚标注为禁用或 demo-only。
- 导航不能暗示不存在的路由已经可用。
- 如果声称有表单能力，就必须有可见的校验、成功和失败状态。
- 相关时必须呈现空态、加载态、错误态和权限态。
- 不把 demo 数据伪装成生产数据。

## 失败处理

修复交互、诚实标注为 mock，或把完成声明降级为 `PARTIAL`。
