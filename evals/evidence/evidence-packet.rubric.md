# Evidence Packet 评分规则（Rubric）

Evidence Packet 必须包含：

- artifact paths；
- 已运行检查，或未运行原因；
- 适用时的人工验证；
- open risks；
- `PASS` / `PARTIAL` / `BLOCKED` completion claim。

如果没有命令输出，也没有人工验证步骤，则 completion claim 不能是 `PASS`。
