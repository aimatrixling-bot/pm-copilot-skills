# Artifact Index Schema

```yaml
artifacts:
  - artifact_id:
    artifact_type:
    path:
    status: draft | working | current | archived
    source:
    key_decisions:
    next_skill_hint:
    last_updated:
```

Use the index to know what can be reused and what should be reviewed before reuse.
